你将为现有项目新增独立的 SLE 页面与脚本，避免在 index.html/main.js 继续膨胀。现状：index.html + js/main.js 已完成 SLB 的设备信息/设置/升级/扫描与主题切换；css/style.css 共用。导航切换依赖 .nav-link/.page-section。

需求：
1) 新建 sle.html：复用 tailwind、font-awesome、css/style.css，复制主题切换（data-theme + 首屏初始化脚本 + 日/月按钮）及整体布局容器。内部定义 SLE 专用 section（如 sle-device-display, sle-device-settings, sle-firmware-upgrade, sle-node-scan），ID/Class 带 sle 前缀，结构可先复制 SLB 版本占位。
2) 新建 js/sle.js：独立全局状态与 API_CONFIG_SLE（从 config_sle.json 或 config.json.sle 读取，若无则 TODO 占位）。独立元素收集、事件绑定、自动刷新/轮询、表单校验、表格渲染、通知复用 showNotification 样式/逻辑（可复制 main.js 里的实现并前缀化变量）。不要引用 main.js 全局，避免冲突。
3) 主题：沿用现有 dark/light 方案，sle.html 中包含与 index.html 相同的 data-theme 覆盖和切换按钮（桌面和移动端），确保 toggle 图标与持久化逻辑一致。
4) 导航与路由：sle.html 内部使用自己的 .nav-link/.page-section 切换，不影响 index.html。主站可在入口新增一个链接到 sle.html（可留 TODO）。
5) 输出：用 apply_patch（或清晰锚点）创建/修改文件：
   - 新建 sle.html（完整骨架、主题脚本、SLE sections、引入 js/sle.js）
   - 新建 js/sle.js（SLE 全量逻辑，独立状态和 API 配置）
   - 如需，新增 config_sle.json 示例（可选）
6) 不破坏 index.html/main.js 现有逻辑，不共享全局定时器或状态。